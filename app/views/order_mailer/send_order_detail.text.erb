
Purchase Details

Name: <%= "#{@order.first} #{@order.last}"%>

Order Number: <%= @order.id %> 
Order Date: <%= @order.created_at.strftime("%Y/%m/%d") %> 
Shipping Address
  Country: <%= @order.country %> 
  Zip: <%= @order.zip %>
  State: <%= @order.state %>
  Address: <%= "#{@order.address1} #{@order.address2}" %>

Product List:

<% @order.order_details.each do |order_product| %>
  <%= "#{order_product.product_name} price: $#{order_product.price} Quantity: #{order_product.quantity}" %>
<% end %>

Payment Method: <%= payment_methods_display(@order.payment_methods) %> 
Billing Amount: <%= "¥#{@order.billing_amount}" %> 